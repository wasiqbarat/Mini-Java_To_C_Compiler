package ast.stmt;

import ast.Statement;

/** Represents a {@code break;} statement. */
public final class BreakStmt implements Statement {
    @Override public String toString() { return "break;"; }
}
