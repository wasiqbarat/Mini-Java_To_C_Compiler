package ast.stmt;

import ast.Statement;

/** Represents a {@code continue;} statement. */
public final class ContinueStmt implements Statement {
    @Override public String toString() { return "continue;"; }
}
